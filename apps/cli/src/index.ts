// Placeholder — Ikary CLI for manifest validation, compilation, and code generation.
//
// Intended commands:
//   ikary validate <manifest.yaml>   — load and validate a manifest
//   ikary compile <manifest.yaml>    — compile to normalized JSON
//   ikary generate <manifest.yaml>   — generate code from manifest
export {};
