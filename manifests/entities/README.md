# Entity Definitions

This directory holds standalone entity YAML fragments that can be composed into full Cell manifests.

Each file defines a single entity (fields, relations, computed fields, lifecycle, capabilities, policies) and can be imported by manifest files via a future merge mechanism.

Currently empty -- entity definitions are inline within example manifests under `../examples/`.
