---
"@ikary/primitives": patch
---

Fix dark mode: `ChartHeader` and `ChartTooltipContent` now use CSS custom properties instead of hardcoded hex values, so chart titles and tooltips render correctly in dark-themed host apps.
