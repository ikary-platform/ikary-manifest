---
"@ikary/primitive-studio": minor
---

Add dark mode support: all inline styles now use CSS custom properties (`hsl(var(--background))`, `hsl(var(--foreground))`, `hsl(var(--border))`, `hsl(var(--muted))`, `hsl(var(--muted-foreground))`, `hsl(var(--accent))`, `hsl(var(--accent-foreground))`) so the studio adapts automatically to the host app's colour scheme.
