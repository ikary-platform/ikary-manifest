# v0.1.1

Package scope rename. No new features or behaviour changes.

## CLI

- Renamed the npm package scope from `@ikary-manifest/cli` to `@ikary/cli`. Update existing installs: `npm install -g @ikary/cli`.
