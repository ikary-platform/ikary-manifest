{
  "name": "ikary-manifest",
  "version": "0.0.1",
  "private": true,
  "description": "Ikary Manifest — open-source declarative cell contracts, runtime engine, and React renderer",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently -n docs,api -c blue,green \"pnpm docs:dev\" \"pnpm --filter @ikary/mcp-server dev\"",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "vitest run --workspace vitest.workspace.ts",
    "test:e2e": "vitest run --project @ikary/mcp-server-e2e",
    "lint": "turbo run lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "coverage:contracts": "vitest run --coverage --root libs/contract && vitest run --coverage --root libs/loader && vitest run --coverage --root libs/engine",
    "coverage:ui": "vitest run --coverage --root libs/presentation && vitest run --coverage --root libs/primitives && vitest run --coverage --root libs/data && vitest run --coverage --root libs/renderer",
    "coverage:python": "cd contracts/python && python3 -m pytest --cov=ikary_manifest --cov-report=term-missing --cov-fail-under=100 tests/",
    "generate:schema": "tsx libs/scripts/generate-json-schema.ts",
    "playground:build": "pnpm --filter @ikary/playground run build",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build && changeset publish",
    "docs:dev": "vitepress dev docs",
    "docs:build": "pnpm playground:build && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@vitest/coverage-v8": "^3.0.0",
    "concurrently": "^9.2.1",
    "jsdom": "^24.1.3",
    "prettier": "^3.3.0",
    "tsup": "^8.0.2",
    "tsx": "^4.19.0",
    "turbo": "^2.8.0",
    "typescript": "^5.5.3",
    "vitepress": "^1.6.0",
    "vitest": "^3.0.0",
    "zod-to-json-schema": "^3.24.0"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.9"
}
