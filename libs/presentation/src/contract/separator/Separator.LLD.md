# Separator LLD

Renders as a `<div>` with `role="separator"` (or `role="none"` when decorative).
Horizontal: `h-[1px] w-full`. Vertical: `h-full w-[1px]`. Color: `bg-border`.
No Radix dependency — pure HTML/Tailwind.
