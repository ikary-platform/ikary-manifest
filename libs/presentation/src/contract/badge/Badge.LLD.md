# Badge LLD

Renders as a `<span>` with rounded-full border. Pure Tailwind, no Radix dependency.
Variant classes map directly to shadcn's badge.tsx CVA variants.
No interactive behavior — Badge is display-only.
