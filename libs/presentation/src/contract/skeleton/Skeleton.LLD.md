# Skeleton LLD

Renders `count` `<div>` elements with `animate-pulse rounded-md bg-muted`.
Height and width are applied via the `heightClass`/`widthClass` props (Tailwind utilities passed as strings).
Default dimensions: `h-4 w-full`. Multiple rows separated by a gap via `flex flex-col gap-2`.
