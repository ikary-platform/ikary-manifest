# Alert LLD

Renders a `<div role="alert">` with shadcn's alert CSS classes.
Icon is chosen based on variant: Info icon for default, AlertCircle for destructive.
Uses lucide-react icons. No Radix dependency.
Title uses `<h5>`, description uses `<p>`.
