# Label LLD

Renders a `<label>` with `htmlFor` wired to `for` attribute.
When `required` is true, appends `<span aria-hidden="true">*</span>` in destructive color.
Uses shadcn label CSS: `text-sm font-medium leading-none peer-disabled:cursor-not-allowed peer-disabled:opacity-70`.
