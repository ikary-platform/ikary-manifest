$schema: 'http://json-schema.org/draft-07/schema#'
title: LineChartPresentation
description: Presentation contract for the line-chart primitive.
type: object
required: [type, data, xKey, series]
additionalProperties: false
properties:
  type:
    type: string
    const: line-chart
  data:
    type: array
    minItems: 1
    items:
      type: object
      additionalProperties:
        oneOf:
          - type: string
          - type: number
          - type: 'null'
  xKey:
    type: string
    minLength: 1
  series:
    type: array
    minItems: 1
    items:
      $ref: '#/definitions/ChartSeries'
  title:
    type: string
    minLength: 1
  description:
    type: string
    minLength: 1
  height:
    type: integer
    minimum: 100
    maximum: 800
  showGrid:
    type: boolean
  showLegend:
    type: boolean
  legendPosition:
    $ref: '#/definitions/ChartLegendPosition'
  showTooltip:
    type: boolean
  strokeWidth:
    type: number
    minimum: 1
    maximum: 10
  showDots:
    type: boolean
  curved:
    type: boolean

definitions:
  ChartSeries:
    type: object
    required: [dataKey, label]
    additionalProperties: false
    properties:
      dataKey:
        type: string
        minLength: 1
      label:
        type: string
        minLength: 1
      color:
        type: string
  ChartLegendPosition:
    type: string
    enum: [top, right, bottom, left, none]
