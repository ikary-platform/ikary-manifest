# Button LLD

Renders a `<button>` element. When `loading` is true, shows an InlineSpinner and disables interaction.
CSS follows shadcn button.tsx CVA variants exactly.
`buttonType` maps to the HTML `type` attribute (defaults to "button").
No Radix dependency — pure HTML/Tailwind.
