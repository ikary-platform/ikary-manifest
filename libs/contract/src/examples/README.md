# Examples

Example manifests have moved to the shared `manifests/examples/` directory at the repository root.

This allows both TypeScript and Python runtimes to consume the same YAML-authored examples.

See: `../../../../../../manifests/examples/`
